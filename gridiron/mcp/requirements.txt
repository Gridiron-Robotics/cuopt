# Deps for the Gridiron Contract-A MCP overlay (gridiron/mcp).
#
# Pinned exactly (estate rule: no floating versions). Only needed where the MCP
# surface runs standalone as the sidecar (gridiron.mcp.app:app) — that install
# needs no GPU and no cuOpt runtime. In a CO-LOCATED deploy the router mounts on
# the upstream cuOpt server, which already brings fastapi and pins
# uvicorn==0.34.* itself (python/cuopt_server/pyproject.toml); do NOT install this
# file there, or the uvicorn pin below fights upstream's.
#
# These are the versions the SIDECAR DEPLOY installs. They are deliberately not
# claimed to be the versions the tests ran against: this checkout's CI image
# carries newer ones (fastapi 0.139.x / httpx 0.28.x / uvicorn 0.50.x as of the
# last run), and the overlay suite passes on both. The earlier wording here
# ("the versions the overlay tests are run against") was simply untrue, and a
# false provenance claim in a pin file is worse than no claim — it is exactly
# what someone reads instead of checking. If you need deploy/test parity,
# install this file into the test env and re-run `gridiron/verify.sh`; do not
# adjust the sentence to match a hope.
fastapi==0.104.1
uvicorn==0.24.0
httpx==0.25.1
